# @biji/cli (biji)

CLI for Get笔记 (biji.com): write / link / search / kb / upload / export / chat / queue, plus `biji doctor`, `biji setup add <client>`, and `biji --ai`.

```bash
npx -y @biji/cli auth login
```

See the monorepo README for full usage.
